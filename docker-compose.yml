version: '3.8'
services:
  ai-challenge:
    build: .
    container_name: ai-challenge
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - ./server/data:/app/server/data
    restart: unless-stopped
